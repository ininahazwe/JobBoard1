<form method="post" action="{{ path('blog_delete', {'id': blog.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button data-text="Supprimer"><span class="la la-trash"></span></button>
</form>
