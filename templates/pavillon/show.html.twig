{% extends 'base.html.twig' %}

{% block title %}Pavillon{% endblock %}

{% block body %}
    <h1>Pavillon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ pavillon.nom }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ pavillon.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ pavillon.createdAt ? pavillon.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ pavillon.updatedAt ? pavillon.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pavillon_index') }}">back to list</a>

    <a href="{{ path('pavillon_edit', {'id': pavillon.id}) }}">edit</a>

    {{ include('pavillon/_delete_form.html.twig') }}
{% endblock %}
