{% extends 'base-dashboard.html.twig' %}

{% block title %}Création de Newsletter{% endblock %}

{% block body %}
    <h1>Ecrivez votre newsletter</h1>
    {{form_start(form)}}
    {{form_row(form.name)}}
    {{form_row(form.content)}}
    {{form_row(form.categorie)}}
    {{form_end(form)}}
{% endblock %}